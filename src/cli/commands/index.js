// @flow
import * as bale from './bale';

export {
	bale
};

// aliases
export const aliases = {
  'bail': 'bale', // I'm pretty sure it's spelt bale but
  'build': 'bale', // for serious developers who don't like a good play on words
};
